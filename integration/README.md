# Integration surfaces

These files are deliberately local preparation for later family integration.
They do not register providers, edit sibling repositories, or publish pressure
records to Commons. The exact revisions used to write them are in
[`../docs/baseline/revisions.json`](../docs/baseline/revisions.json).
